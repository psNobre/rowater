# Rowater
Manage rounds.
## Installation
1. Clone it.
2. Install dependencies npm: 
    `cd rowater`
    `npm install`
3. Install dependencies bower: 
    `cd public`
    `bower install`   
4. Running application: 
    `cd ..`
    `node server.js`
